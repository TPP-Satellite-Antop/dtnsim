cmake_minimum_required(VERSION 3.10) project(dtnsim CXX)

    set(CMAKE_CXX_STANDARD 17)

        include_directories(.include / usr / include / python3 .11 ${OMNETPP_INCL_DIR})

            file(GLOB_RECURSE SOURCES src /**/ *.cc)

                add_executable(dtnsim ${SOURCES})

#Si querés agregar warnings tipo clang - tidy
                    set(CMAKE_CXX_CLANG_TIDY "clang-tidy;-checks=*")

                        target_link_libraries(dtnsim python3 .11)
